import {
  SafeAreaView,
  StyleSheet,
  Text,
  View,
  Image,
  Button,
  Pressable,
  ScrollView,
} from "react-native";

import colors from "../config/colors";

export default function FriendsScreen() {
  return (
    <SafeAreaView>
      <Text>Friends Screen</Text>
    </SafeAreaView>
  );
}
